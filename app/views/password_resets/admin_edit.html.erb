<h1>Reset Your Password(Admin)</h1>

<%= form_for :admin, url: admin_password_reset_edit_path(token: params[:token]) do |form| %>
  <%= render partial: 'edit_form', locals: {form: form} %>
<% end %>