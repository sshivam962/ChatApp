<!DOCTYPE html>
<html>
<head>
  <style>
      table {
          border-collapse: collapse;
          width: 100%;
      }
      th, td {
          padding: 8px;
          text-align: left;
          border-bottom: 1px solid #ddd;
      }
      tr:hover {background-color: #d7d2d2;}
  </style>
</head>
<body>

  <table>
    <% if @projects.present? %>
      <tr>
        <th>Project Name</th>
        <th>Show</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
        <% @projects.each do |project| %>
          <tr>
            <td><%= project.project_name %></td>
            <td><%= link_to 'Show', project_path(project) %></td>
            <td><%= link_to 'Edit', edit_project_path(project) %></td>
            <td> <%= link_to "Delete", project_path(project),
                                 method: :delete,
                                 data: { confirm: "Are you sure?" } %></td>
          </tr>
        <% end %>
    <% else %>
      <h4>No projects found. <%= link_to 'Click here', '/admin' %> to go back.</h4>
    <% end %>
  </table>
  <h6><%= link_to 'New Project', new_project_path %></h6>

</body>
</html>